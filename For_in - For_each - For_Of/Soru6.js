// SORU 6 
// Bir dizi içindeki her elemanı iki katına çıkarıp ekrana yazdırın.

let numbers = [1, 2, 3, 4, 5];

numbers.forEach(function(number) {
    let doubled = number * 2;
    console.log(doubled);
});
